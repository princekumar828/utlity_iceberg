# Environment Variables Template
# Copy this file to .env and update with your actual lakehouse connection details

# Required Configuration - UPDATE THESE WITH YOUR ACTUAL VALUES
NESSIE_URI=http://your-nessie-host:19120/api/v1
S3_ENDPOINT=http://your-minio-host:9000
S3_ACCESS_KEY=your_actual_access_key
S3_SECRET_KEY=your_actual_secret_key
WAREHOUSE_PATH=s3://your-warehouse-bucket/

# Optional Configuration
NESSIE_REF=main
S3_REGION=us-east-1
SSL_VERIFY=false

# Authentication (if required)
# NESSIE_AUTH_TYPE=basic
# NESSIE_USERNAME=your_username
# NESSIE_PASSWORD=your_password

# Usage:
# 1. Copy this file: cp .env.template .env
# 2. Update the values in .env with your actual configuration
# 3. Load the environment: source .env (or use a tool like direnv)
# 4. Run the explorer: python main.py overview
